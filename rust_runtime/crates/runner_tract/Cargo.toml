[package]
name = "runner_tract"
version = "0.1.0"
edition = "2021"

[dependencies]
tract-onnx = { version = "0.20", default-features = false, features = ["onnx"] }
ndarray = "0.15"
anyhow = "1.0"
tract-ndarray = "0.20"
