[package]
name = "runner_tract"
version = "0.1.0"
edition = "2021"
description = "ONNX inference runner using the pure-Rust tract engine."

[dependencies]
runner_core = { path = "../runner_core" }
tract-onnx = "0.20"
ndarray = "0.15"
anyhow = "1.0"
