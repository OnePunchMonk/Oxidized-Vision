[package]
name = "runner_tensorrt"
version = "0.1.0"
edition = "2021"
description = "TensorRT inference runner (experimental) for NVIDIA GPU acceleration."

[dependencies]
runner_core = { path = "../runner_core" }
anyhow = "1.0"
ndarray = "0.15"
