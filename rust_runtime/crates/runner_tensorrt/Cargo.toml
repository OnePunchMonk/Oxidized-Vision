[package]
name = "runner_tensorrt"
version = "0.1.0"
edition = "2021"

[dependencies]
# This assumes a TensorRT binding crate exists. If not, one would need to be created.
# For this example, we'll imagine a crate named `tensorrt_rs`.
# tensorrt_rs = "0.1" 
anyhow = "1.0"
ndarray = "0.15"
