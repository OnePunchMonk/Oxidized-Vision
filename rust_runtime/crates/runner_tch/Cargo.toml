[package]
name = "runner_tch"
version = "0.1.0"
edition = "2021"
description = "TorchScript inference runner using LibTorch bindings."

[dependencies]
runner_core = { path = "../runner_core" }
tch = "0.10"
ndarray = "0.15"
anyhow = "1.0"
