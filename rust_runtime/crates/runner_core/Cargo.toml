[package]
name = "runner_core"
version = "0.1.0"
edition = "2021"
description = "Shared trait and types for OxidizedVision inference runners."

[dependencies]
ndarray = "0.15"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
